<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Editar Vivienda</title>
</head>
<body>
  <h1>Editar Vivienda</h1>
  <form action="/viviendas/<%= vivienda._id %>" method="POST">
    <input type="hidden" name="_method" value="PUT">
    <div>
      <label for="descripcion">Descripción:</label>
      <input type="text" id="descripcion" name="descripcion" value="<%= vivienda.descripcion %>" required>
    </div>
    <div>
      <label for="barrio">Barrio:</label>
      <input type="text" id="barrio" name="barrio" value="<%= vivienda.barrio %>" required>
    </div>
    <div>
      <label for="calle">Calle:</label>
      <input type="text" id="calle" name="calle" value="<%= vivienda.calle %>" required>
    </div>
    <div>
      <label for="numero">Número:</label>
      <input type="number" id="numero" name="numero" value="<%= vivienda.numero %>" required>
    </div>
    <div>
      <label for="piso">Piso:</label>
      <input type="text" id="piso" name="piso" value="<%= vivienda.piso %>" required>
    </div>
    <div>
      <label for="propietario">Propietario:</label>
      <input type="text" id="propietario" name="propietario" value="<%= vivienda.propietario %>" required>
    </div>
    <div>
      <label for="precio">Precio:</label>
      <input type="text" id="precio" name="precio" value="<%= vivienda.precio %>" required>
    </div>
    <button type="submit">Guardar cambios</button>
  </form>
</body>
</html>
